// Dont use These things 

// eval
//with 
// with eval and with, have an issue with the scope, it will internally change the scope

//arguments
//arguments is little dangerous to use, because its not an array, n exactly an array because we cannot really use
// array methods on the arguments keyword 

//for in
// delete